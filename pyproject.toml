[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week2_bacasss"
version = "0.0.4"
dependencies = ["requests"]

[project.scripts]
song = "resolution_week2_bacasss.main:main"