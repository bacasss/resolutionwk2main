[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week2_bacasss"
version = "0.0.1"
dependencies = ["requests"]

[project.scripts]
cmd = "resolution_week2_bacasss.main:main"